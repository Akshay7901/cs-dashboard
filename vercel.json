{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bun run build && node scripts/build-vercel.mjs",
  "installCommand": "bun install",
  "outputDirectory": "dist/client",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}